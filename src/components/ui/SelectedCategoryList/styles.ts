import { StyleSheet } from 'react-native';

import { hp } from 'src/utils';

export const styles = StyleSheet.create({
  list: {
    width: '100%',
  },
  contentContainer: {
    gap: hp(16),
  },
  footer: {
    height: hp(200),
  },
});
